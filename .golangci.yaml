# options for analysis running
run:
  timeout: 2m
  skip-files:
    - "zz_generated.*"
linters:
  enable:
    - golint
    - whitespace
    - wsl
    - goimports
linters-settings:
  goimports:
    local-prefixes: github.com/gardener/gardenctl-v2
